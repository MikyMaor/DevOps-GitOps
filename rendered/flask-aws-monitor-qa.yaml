---
# Source: flask-aws-monitor/templates/service.yaml
apiVersion: v1
kind: Service

metadata:
  name: flask-qa-flask-aws-monitor
  labels:
    helm.sh/chart: flask-aws-monitor-0.1.0
    app.kubernetes.io/name: flask-aws-monitor
    app.kubernetes.io/instance: flask-qa
    app.kubernetes.io/version: "1.0.0"
    app.kubernetes.io/managed-by: Helm

spec:
  type: ClusterIP

  selector:
    app.kubernetes.io/name: flask-aws-monitor
    app.kubernetes.io/instance: flask-qa

  ports:
    - protocol: TCP
      port: 5001
      targetPort: http
---
# Source: flask-aws-monitor/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment

metadata:
  name: flask-qa-flask-aws-monitor
  labels:
    helm.sh/chart: flask-aws-monitor-0.1.0
    app.kubernetes.io/name: flask-aws-monitor
    app.kubernetes.io/instance: flask-qa
    app.kubernetes.io/version: "1.0.0"
    app.kubernetes.io/managed-by: Helm

spec:
  replicas: 2

  selector:
    matchLabels:
      app.kubernetes.io/name: flask-aws-monitor
      app.kubernetes.io/instance: flask-qa

  template:
    metadata:
      labels:
        app.kubernetes.io/name: flask-aws-monitor
        app.kubernetes.io/instance: flask-qa

    spec:
      containers:
        - name: flask-aws-monitor

          image: "miky97/flask-aws-monitor:2"

          imagePullPolicy: Always

          ports:
            - name: http
              containerPort: 5001

          env:
            - name: AWS_ACCESS_KEY_ID
              value: ""

            - name: AWS_SECRET_ACCESS_KEY
              value: ""

          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 2
            periodSeconds: 5

          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10

          resources:
            limits:
              cpu: 300m
              memory: 512Mi
            requests:
              cpu: 100m
              memory: 128Mi
