replicaCount: 1

image:
  repository: miky97/flask-aws-monitor
  tag: "2"
  pullPolicy: Always

service:
  type: ClusterIP
  port: 5001

ingress:
  enabled: false

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 200m
    memory: 256Mi
